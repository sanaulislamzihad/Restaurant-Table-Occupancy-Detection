# Dashboard settings. Copy to frontend/.env to change them (.env overrides this file).

# Address of the backend: API_HOST and API_PORT from backend/.env.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Port of the dashboard dev server. It must be listed in CORS_ORIGINS in backend/.env.
FRONTEND_PORT=5173
